#version 430 core

layout (location=0) in vec3 vertex_pos;
layout (location=1) in vec2 tex_coords;

out vec3 out_color;
out vec2 vertex_tex_coord;

void main(){
	gl_Position = vec4(vertex_pos, 1.0);
	out_color = vec3(0.0, 1.0, 0.0);
	vertex_tex_coord = tex_coords;
}